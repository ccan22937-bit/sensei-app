      <AnimatePresence>
        {selectedChestDay !== null && (
          <motion.div 
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
            className="fixed inset-0 z-50 bg-black/80 backdrop-blur-sm flex items-center justify-center p-4"
          >
            <motion.div 
              initial={{ scale: 0.9, y: 20 }}
              animate={{ scale: 1, y: 0 }}
              exit={{ scale: 0.9, y: 20 }}
              className="bg-[#1A1A24] rounded-3xl w-full max-w-sm p-6 relative flex flex-col items-center text-center shadow-[0_0_30px_rgba(0,240,255,0.1)] border border-white/10 overflow-hidden"
            >
              <button 
                onClick={() => { if (!wheelSpinning) setSelectedChestDay(null) }}
                className="absolute top-4 right-4 text-gray-400 hover:text-white bg-white/5 rounded-full p-2 transition-colors z-10"
              >
                <X size={20} />
              </button>

              <div className="w-24 h-24 bg-[#00F0FF]/10 border border-[#00F0FF]/30 text-[#00F0FF] rounded-full flex items-center justify-center mb-6 shadow-[0_0_15px_rgba(0,240,255,0.2)]">
                <Gift fill="currentColor" size={48} />
              </div>

              <h2 className="text-2xl font-bold text-white mb-2">{t(nativeLanguage, 'chest_title') || 'Şans Kutusu'}</h2>
              <p className="text-gray-400 mb-8">{t(nativeLanguage, 'chest_desc') || 'Şans çarkını çevir ve ödülünü kazan!'}</p>

              {wheelResult === null ? (
                <div className="relative w-48 h-48 mb-8 flex items-center justify-center">
                  <motion.div 
                    animate={wheelSpinning ? { rotate: 3600 } : { rotate: 0 }}
                    transition={wheelSpinning ? { duration: 3, ease: "circOut" } : { duration: 0 }}
                    className="w-full h-full border-4 border-dashed border-[#00F0FF] rounded-full flex items-center justify-center"
                  >
                    <span className="text-[#00F0FF] font-bold text-xl">{wheelSpinning ? '...' : '?'}</span>
                  </motion.div>
                  <div className="absolute -top-2 w-4 h-4 bg-white rounded-full shadow-[0_0_10px_white]"></div>
                </div>
              ) : (
                <motion.div 
                  initial={{ scale: 0, opacity: 0 }}
                  animate={{ scale: 1, opacity: 1 }}
                  className="mb-8 flex flex-col items-center"
                >
                  <div className="flex items-center gap-2 text-4xl font-black text-[#FF4B4B] drop-shadow-[0_0_15px_rgba(255,75,75,0.5)]">
                    +{wheelResult} <Heart fill="currentColor" size={40} />
                  </div>
                  <p className="text-[#FF4B4B] font-bold mt-2">Tebrikler!</p>
                </motion.div>
              )}

              {wheelResult === null ? (
                <button 
                  onClick={spinWheel}
                  disabled={wheelSpinning}
                  className="w-full bg-[#00F0FF] hover:bg-[#00D0DD] text-black font-bold py-4 rounded-2xl shadow-[0_0_20px_rgba(0,240,255,0.4)] border-b-4 border-[#00A0AA] active:border-b-0 active:translate-y-1 transition-all disabled:opacity-50 text-lg"
                >
                  {wheelSpinning ? 'Çevriliyor...' : 'Çevir'}
                </button>
              ) : (
                <button 
                  onClick={claimChest}
                  className="w-full bg-[#FF4B4B] hover:bg-[#FF6B6B] text-white font-bold py-4 rounded-2xl shadow-[0_0_20px_rgba(255,75,75,0.4)] border-b-4 border-[#CC0000] active:border-b-0 active:translate-y-1 transition-all text-lg flex items-center justify-center gap-2"
                >
                  <Heart fill="currentColor" size={24} /> Al
                </button>
              )}
            </motion.div>
          </motion.div>
        )}
      </AnimatePresence>
